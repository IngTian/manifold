build/
*.o
*.swiftmodule
.DS_Store
frames/

# Local dev scratchpads — interactive rigs that re-implement the terrain math in
# JS to explore looks/palettes. Handy locally, but they'd need hand-syncing with
# the Swift renderer, so they're deliberately not published.
tools/terrain-explorer.html
tools/palette-preview.html
tools/palette-designs.json
